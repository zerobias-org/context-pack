{
  "name": "@zerobias-org/context-pack-root",
  "private": true,
  "type": "module",
  "description": "Shared toolchain packs for ZeroBias content packages",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "_comment": [
    "This root declares NO dependencies on purpose, and this repo is NOT an npm",
    "workspace \u2014 each pack under package/ resolves and locks its own deps, the",
    "same shape as auditlogic/module.",
    "",
    "Why it matters: Prepublish.resolve() (zb.monorepo-*) rewrites a package's",
    "dependency versions from the ROOT package.json at publish time. With no",
    "workspace root carrying deps there is nothing to hoist, so the packs' pins",
    "survive structurally rather than by convention. Enforced by the root's",
    "verifyRootHasNoDeps task."
  ]
}
